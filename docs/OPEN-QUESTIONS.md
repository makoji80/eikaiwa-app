# 未決事項（仕様書 v2.0 セクション13）

仕様書は「未決でもローカルのPhase 0・テキスト試作は進められる」としているため、以下はデフォルト値・仮実装で進めた。**外部公開・課金実行の前に必ずユーザー確認が必要**。

| # | 項目 | 現状の仮決定・実装内容 | 誰が決めるか |
|---|---|---|---|
| 1 | 新規プロジェクトの保存先 | 本セッションの作業リポジトリを起点として実装。GitHub等の実リモートへのpush・旧アプリとの物理的な配置関係はユーザー自身の環境（Mac）で判断 | ユーザー |
| 2 | LINEログインを開発時から必須にするか | 未実装。開発用に自前 email+password + JWT のdev認証のみ実装。本番前に差し替え | ユーザー |
| 3 | 開発版のみか有料一般公開まで進めるか | 現状は開発版（ローカル実行、外部公開なし） | ユーザー |
| 4 | 音声・会話履歴の保存範囲/保存期間/削除/AI提供者への送信方針 | 音声フェーズ実装済みだが録音データは一切保存しない設計（[decisions/0002](decisions/0002-voice-phase.md)）。保存が必要になった場合はSpeechAssetテーブル配線・オブジェクトストレージ選定から要検討 | ユーザー |
| 5 | 月額原価上限・想定利用時間・課金方式 | 未決。UsageEventテーブルへの記録のみ実装（予算アラート等は未実装） | ユーザー |
| 6 | サービス名・優先ユーザー層・ビジネス英語/日常会話の優先度 | 仮称「eikaiwa」を使用。優先度は特に設けず両対応の設計 | ユーザー |

## 今回の実装で新たに生じた既知のギャップ（次フェーズで対応）

- ログイン試行のレート制限は簡易版のみ（本格的な濫用対策は未実装）
- JWTの失効・リフレッシュ戦略なし（有効期限切れ後は再ログインが必要）
- ログの秘密値マスキングは基本的なredactのみ
- AI利用量/コスト元帳（UsageEvent）は書き込みのみ。予算上限アラート・機能別の品質調整は未実装
- 冪等性: プロセスがAI呼び出し中にクラッシュした場合、`processing`状態のレコードは一定時間で失効し再試行可能になる設計（要求の完全な exactly-once 保証ではない）
- 実機（iOS/Android）での動作確認は本サンドボックスでは実施不可。[docs/device-verification/voice-phase-checklist.md](device-verification/voice-phase-checklist.md) にユーザー実施用のチェックリストを用意
- 音声: 連続対話・割り込み(バージイン)は未実装（押して話す方式のみ）。無音/短すぎる録音の検知はバイトサイズ閾値のみの簡易判定。OpenAI Whisper連携は実鍵での検証ができていない
