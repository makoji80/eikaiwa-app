{
  "name": "@eikaiwa/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "英会話アプリの API 契約・ドメイン型（Zodスキーマ）。apps/mobile と apps/api が共有する唯一のソース。",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
